apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: frontend-virtual-service
spec:
  hosts:
    - frontend
  http:
    - route:
        - destination:
            host: frontend
            subset: green
          weight: 50
        - destination:
            host: frontend
            subset: blue
          weight: 50
